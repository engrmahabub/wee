-------------------------------
-- Fibonacci function fib()
-------------------------------
#driver "fi"

fib(n ∈ N) => y ∈ N: 
  is (n = 1 ∨ n = 0)? 
    set y := 1
  no:
    set y := fib(n - 1) + fib(n - 2)
  is;
fib.

-- use function fib    
put fib(5)
put fib(10)
  
write 

end.